# 62.	WAP to Count distinct elements of an array 

list = list(map(int, input("Enter the elements of the array separated by space: ").split()))

print("Distinct elements in the array:", set(list))